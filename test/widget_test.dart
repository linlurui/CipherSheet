import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test — boots without crash', (WidgetTester tester) async {
    // Placeholder: full widget tests require mocking LicenseService FFI.
    expect(true, isTrue);
  });
}
